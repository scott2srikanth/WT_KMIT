create database wt2020;

use wt2020;

create table Users(user_id integer not null auto_increment primary key,
username varchar(40),password varchar(10),
fullname varchar(100),email varchar(100));


insert into users(username,password,fullname,email) 
values('scott','12345','scott reddy','abc@abc.com');

select * from users;